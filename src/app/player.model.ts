export class Player {
  constructor(public username: string, public level: number, public idleAttack: number, public attack: number, public critChance: number, public criticalDamage: number, public gold: number, public id: number){ }
}
